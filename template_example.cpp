#include <iostream>
#include <vector>
#include <algorithm>

// Functor that adds a given value to each element
template<typename T>

T my_max(const T& a, const T& b){
	
	if a > b{
		
		return a;
	} else {
		return b;
	}
} 

int main() {
    
	my_max(1,2);
    return 0;
}
